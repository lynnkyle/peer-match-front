# 前端
# user-center
server {
    listen 80;
    server_name user-center-front.linzeyuan.site;
    # gzip
    gzip on;
    gzip_min_length 1k;
    gzip_comp_level 9;
    gzip_types text/plain text/css text/javascript application/json application/javascript application/x-javascript application/xml;
    gzip_vary on;
    gzip_disable "MSIE [1-6]\.";

    root /usr/share/nginx/html/user-center;
    include /etc/nginx/mime.types;
    location / {
        try_files $uri /index.html;
    }
}
# peer-match
server {
    listen 80;
    server_name peer-match-front.linzeyuan.site 118.25.157.191;
    # gzip
    gzip on;
    gzip_min_length 1k;
    gzip_comp_level 9;
    gzip_types text/plain text/css text/javascript application/json application/javascript application/x-javascript application/xml;
    gzip_vary on;
    gzip_disable "MSIE [1-6]\.";

    root /usr/share/nginx/html/peer-match;
    include /etc/nginx/mime.types;
    location / {
        try_files $uri /index.html;
    }
}
# 后端(反向代理)
